version: "3"

services:
  web:
    build: ./web/
    container_name: cryptpaste-web
    restart: always
    ports:
      - "3333:3333"
    command:
      "npm run start"

  server:
    build: ./server/
    container_name: cryptpaste-server
    restart: always
    ports:
      - "3000:3000"